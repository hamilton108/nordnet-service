(ns nordnetservice.adapter.downloadadaptertest
  (:require
   [clojure.test :refer [deftest is]]
   [nordnetservice.adapter.downloadadapter :as adapter]))

;; (deftest test-url-for-ticker
;;   (is (= "" (adapter/url-for-ticker ""))))
